[build]
builder = "NIXPACKS"
buildCommand = "pip install --upgrade pip && pip install -r requirements.txt && npx prisma generate"

[build.env]
NIXPACKS_NODE_VERSION = "18"

[start]
cmd = "python -m uvicorn main:app --host 0.0.0.0 --port $PORT"

[variables]
PYTHON_VERSION = "3.10"